<%= form_for(@feedback_type) do |f| %>
  <% if @feedback_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback_type.errors.count, "error") %> prohibited this feedback_type from being saved:</h2>

      <ul>
      <% @feedback_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
